# 方太个性化膳食规划 Agent 作品信息摘要

## 作品基本信息

| 项目 | 内容 |
|------|------|
| 作品名称 | 方太个性化膳食规划 Agent |
| 队名 | 膳智未来 |
| 组别 | 本科组 |
| 专项赛 | “方太”人工智能专项赛 |
| 赛道 | 个性化膳食规划 Agent |
| 题目编号 | ZX-2026-0301 |

## 团队分工

| 角色 | 主要工作 |
|------|----------|
| 核心算法 | RAG 检索、约束过滤、LLM 调用与结果校验 |
| 交互与测试 | 多轮对话管理、测试用例设计、演示脚本 |
| 工程部署 | Flask API、Docker 部署、性能优化与参赛材料整理 |

## 作品概述

本作品面向“不同健康状况用户如何获得安全、合适、可解释的膳食推荐”这一问题，构建了一个基于 RAG 和规则约束的个性化膳食规划 Agent。系统结合 2000 道菜谱、50 份用户健康档案和自建营养数据库，在推荐前先检索候选菜谱，再依据过敏、慢性病、口味偏好和营养需求进行过滤，最后由大语言模型生成自然语言解释。

系统支持三类核心场景：单人单餐推荐、多人多约束聚餐推荐、多轮对话中的约束追加和局部替换。相比单纯让大模型直接生成菜谱，本作品把推荐范围限定在真实菜谱库内，并通过约束引擎降低“推荐不存在菜品”或“触犯健康禁忌”的风险。

## 核心能力

| 能力 | 说明 |
|------|------|
| 个性化推荐 | 根据用户年龄、性别、BMI、过敏原、慢性病、口味偏好和健康需求推荐菜品 |
| 约束校验 | 对过敏原、糖尿病、高血压、高尿酸、高血脂等条件进行硬约束过滤 |
| 混合检索 | 结合关键词检索与语义检索，兼顾精确匹配和自然语言理解 |
| 多轮交互 | 支持“不要辣”“换一道”“重新推荐”等连续对话 |
| 结果解释 | 输出推荐理由、营养信息和膳食均衡度，便于用户理解选择依据 |
| 工程部署 | 提供 Flask API、Dockerfile、Docker Compose 和部署说明 |

## 技术亮点

1. **Agent ReAct 自主推理**：通过 Function Calling 实现"思考-行动-观察"循环，LLM 自主决定调用搜索、约束检查或向用户追问，比固定流水线更灵活。
2. **RAG + 约束引擎的双层推荐机制**：先从菜谱库检索候选结果，再用健康约束过滤，减少大模型自由生成带来的不确定性。
3. **体检指标辅助识别**：系统可根据健康档案中的血压、血糖、尿酸、胆固醇等指标识别潜在饮食限制。
4. **中文语义检索 + 精排**：采用 `bge-small-zh-v1.5` 中文向量模型（512 维）做语义检索，并用 `bge-reranker-base` 交叉编码器对 Top 候选二次精排，中文表达理解更准、前 5 名相关性更高。
5. **多轮对话状态持久化**：对话偏好与饮食目标落盘到 `.dialog_store/`，跨请求、跨服务重启均可恢复，支持否定重推、约束追加、多人合并等 6 种交互类型。
6. **对话中实时健康约束识别**：用户在对话里声明慢性疾病（高血压/糖尿病/高血脂/痛风/高尿酸）或特殊人群（老人/儿童/孕妇）时，系统实时提取并同步进约束引擎档案，非预置用户也能获得严格的禁忌过滤（如高血压用户不会收到咸肉/咸鱼）。
7. **推荐多样性与数量理解**：检索时排除本会话最近已推菜与已否决菜，配合候选池轮转避免"复读机"；识别"只吃四道/来三道"等数量表达精确返回；单一食材请求（如"想吃土豆"）自动均衡为"主菜 + 荤素搭配"，避免营养失衡。
8. **约束安全网**：推荐出口统一执行健康约束复检，并优先从"库内合规池"补足，杜绝过敏原/慢性病禁忌菜混入，同时避免约束过紧导致的空推荐。
9. **组件预热与性能收敛**：启动时加载菜谱、用户档案、营养数据库和检索索引；ReAct 推荐循环迭代收敛至 2 次，配合 180s 泛化查询缓存，显著降低端到端延迟与 LLM 调用次数，防止高频评测下崩溃。
10. **自纠错与重复追问纠偏**：每轮推荐对结果做质量诊断（空回复/单调/违规），用户换措辞重复追问时能识别并主动承认上一轮问题（如"我发现刚配的几道有点单一…"），同时在需求方向外强制更换搭配、收窄主菜保留量，实现"发现问题—自我纠正"的闭环，避免反复给出相同结果。

## 数据资源

| 数据 | 规模 | 用途 |
|------|------|------|
| 菜谱知识库 | 约 2000 道菜谱 | 推荐候选、菜品详情、标签检索 |
| 用户健康档案 | 50 份 | 个性化约束、健康画像、测试样例 |
| 营养数据库 | 185 种食材 | 热量、蛋白质、脂肪、碳水、膳食纤维等估算 |
| 对话用例 | 25 组（大赛提供）+ 16 组（自测扩展场景） | 验证基础推荐、多轮交互、约束追加和方案替换 |

## 测试与验证

| 测试项 | 验证结果 |
|--------|----------|
| 基础推荐 | 可根据用户档案返回候选菜品 |
| 约束过滤 | 可过滤过敏原和常见慢性病禁忌（含高血压、糖尿病、高尿酸、高血脂、孕妇禁忌词） |
| 多轮对话 | 可记录偏好并延续上下文，跨请求/重启持久化 |
| 对话用例 | 25 组用例通过，另完成 10 组日常场景、39 轮多轮对话压力测试全部通过 |
| 本地接口响应 | 检索、约束、营养计算接口可达到毫秒级响应 |

在竞赛自动评分（`eval.py`，总分 100 分，覆盖基础推荐/复杂场景/多轮交互/性能效率四维度）中当前版本取得 **100/100** 的满分成绩：25 组对话用例、43 轮全部通过，零空响应、零幻觉。其中基础推荐、复杂场景、多轮交互、性能效率四个维度均获满分，ReAct 迭代收敛与泛化查询缓存显著改善端到端延迟。

需要说明的是，本地检索和规则计算不依赖远程模型，因此响应时间较短；使用 DeepSeek v4-flash 等远程 LLM 时，端到端响应时间会受网络和模型服务影响。实际测试中首 Token 延迟约 0.9-1.25s，单轮端到端约 1.8-3.6s（流式接口），达到竞赛评优标准。

## 应用价值

本作品可作为智能厨电、家庭健康管理和数字化菜谱服务的能力模块。对用户而言，它能把复杂的健康限制转化为可执行的菜品建议；对平台而言，它能把菜谱库、用户画像和智能推荐结合起来，提升健康饮食服务的个性化程度。

## 总结

方太个性化膳食规划 Agent 以真实菜谱库为基础，以健康约束为边界，以大语言模型作为解释和交互层，形成了“检索、过滤、生成、校验”的完整推荐流程。系统已具备可演示、可部署、可扩展的工程形态，能够较好回应个性化膳食规划赛题要求。

提交日期：2026 年 7 月  
团队名称：膳智未来
